[build]
base = "python:3.11-slim"
dependencies = [
    "opencv-python-headless", 
    "numpy", 
    "torch", 
    "torchvision", 
    "requests", 
    "pandas", 
    "seaborn", 
    "tqdm", 
    "psutil"
]
system_packages = ["libgl1", "libglib2.0-0", "libgomp1"]

[runtime]
gpu = false  # Prototype runs on CPU as per user code
